describe('Test the harvest report dynamic features', () => {
    beforeEach(() => {
        cy.login('manager1', 'farmdata2')
        cy.visit('/farm/fd2-school/fd2')
    })
    it('Check custom table', ()=> {

    })

})
